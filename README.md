## 🚀 Quick start

- The first time you run this project you will need to:
- Run `npm install`
- Then `gatsby develop`
- Every other time then after you will only need to run 'gatsby develop'

View site at: http://localhost:8000
